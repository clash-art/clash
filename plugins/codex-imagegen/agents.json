{
  "summary": "First-party Codex image generation plugin whose Action Card explicitly projects its native Generator Definition over one transport-neutral PluginModule.",
  "context_hints": [
    "Keep Codex CLI execution in the Clash host tool; plugin code calls context.hostTools.codexImagegen.generate",
    "Do not add API-key fallbacks: this Action intentionally uses the user's existing Codex sign-in",
    "Keep generated bytes project-scoped by returning the asset handle from the typed host tool",
    "Model, quality, target dimensions, and background are persisted Action Card preferences composed into the prompt by src/stdio.ts. The native Codex tool contract remains prompt and references only. See apps/docs/guide/gpt-image-25.md."
  ],
  "subdirectories": {
    "cards": "User-facing Action Card parameters and reference-image constraints.",
    "generators": "Native clash.generator/v1 Definition used by generic HTTP/CLI/MCP and mapped Canvas Run admission. Custom Canvas draft authoring is still legacy and has not become a native Revision editor.",
    "src": "Transport-neutral PluginModule plus its compatibility stdio entrypoint and protocol tests.",
    "contract-tests": "Declarative activation-gating ABI fixtures.",
    "dist": "Generated standalone install artifact; never hand-edit."
  },
  "key_files": {
    "manifest.json": "Versioned executable package and contribution declarations.",
    "cards/codex-imagegen.json": "Action Card with aspect-ratio and prompt-driven image preferences. Declares its exact Definition, generate Action, and persistent image input slot; the Host must not infer ownership from the executor function.",
    "generators/codex-imagegen.json": "Native fork-when-materialized Generator Definition with one generate Action and one Media output.",
    "src/stdio.ts": "Exports the inert first-party PluginModule and starts the same module over stdio only when executed directly.",
    "contract-tests/generate-image.json": "Activation contract for asset output."
  },
  "verification": [
    "pnpm test:package @clash-plugin/codex-imagegen",
    "pnpm typecheck:package @clash-plugin/codex-imagegen",
    "pnpm build:package @clash-plugin/codex-imagegen",
    "node packages/cli/dist/index.js action validate plugins/codex-imagegen --json"
  ]
}
