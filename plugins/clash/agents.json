{
  "name": "Clash Distribution",
  "purpose": "Package the CLI, stdio MCP surface, skills, and internal local-api host as the single installable clash npm package.",
  "runtimeBoundary": "The clash dispatcher selects the CLI for ordinary arguments and the peer stdio MCP for `clash mcp`. Both use one discoverable local-api daemon. Discovery includes the content fingerprint of the host artifact: compatible clients reuse that exact daemon, while a superseded artifact is retired under the single startup lock before its replacement publishes discovery. MCP keeps a fixed `clash_plugin` dispatcher whose live operations author drafts in the Agent working tree and manage activated packages through that discovered Host; plugin activation never creates a second daemon or dynamic MCP tool list. Desktop carries the same host runtime and reuses the compatible machine singleton; client or Desktop shutdown does not stop the shared daemon. Source-host file watching is explicit development behavior, not the daemon default. The distribution carries a closed set of immutable first-party plugin payloads that the Host imports in-process; only explicitly activated third-party packages use the actions-directory process/stdio realm.",
  "entrypoints": [
    ".codex-plugin/plugin.json",
    ".mcp.json",
    "tsconfig.typecheck.json",
    "src/dispatcher.ts",
    "src/index.ts",
    "src/server.ts",
    "src/host-runner.ts",
    "src/plugin-host.ts",
    "src/plugin-mcp-gateway.ts",
    "src/local-api-entry.ts",
    "scripts/build-host-runtime.ts",
    "skills/clash/SKILL.md",
    "skills/clash/references/tools.md",
    "skills/clash/references/generation.md",
    "skills/clash/references/workspace.md",
    "skills/clash/references/views.md",
    "skills/clash/references/task-methods.md",
    "skills/clash-director-production/SKILL.md",
    "skills/clash-film-production/SKILL.md",
    "skills/clash-multiview-consistency/SKILL.md",
    "skills/clash-reference-composition/SKILL.md",
    "skills/clash-shot-continuity/SKILL.md",
    "skills/clash-timeline-production/SKILL.md",
    "skills/clash-mg-character/SKILL.md",
    "skills/clash-video-finishing/SKILL.md",
    "skills/clash-cover-image/SKILL.md",
    "skills/clash-infographic/SKILL.md",
    "skills/clash-comic/SKILL.md",
    "skills/clash-product-ad/SKILL.md",
    "skills/clash-talking-head/SKILL.md",
    "skills/clash-explainer/SKILL.md",
    "skills/clash-narrative/SKILL.md",
    "skills/clash-music-video/SKILL.md",
    "skills/clash-brand-motion/SKILL.md",
    "skill-packs.json"
  ],
  "typecheckBoundary": "lint and typecheck use tsconfig.typecheck.json, extending the development workspace aliases so current Local API and MCP contracts are checked instead of stale dist declarations. It uses Bundler resolution for the bundled runtime and DOM Web API types shared by the Host, keeps strictness and the full source/test include set, emits no JavaScript or declarations and sets no heap override. Incremental checker metadata is kept in the package-local ignored .cache/typecheck.tsbuildinfo, without limiting dependency invalidation. Release artifact consistency remains a separate package-boundary test.",
  "verification": [
    "pnpm test:package clash",
    "pnpm typecheck:package clash",
    "pnpm build:package clash"
  ]
}
