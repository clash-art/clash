{
  "summary": "Application shell components, Context Providers, and React Flow views used within the Next.js App Router.",
  "context_hints": [
    "ProjectEditor.tsx composes the workspace shell; feature business logic belongs under src/features",
    "Contains all Global State Providers (Contexts)",
    "Contains the 'Nodes' implementation for React Flow",
    "GlobalAssetsClient uses the shared personal Global Asset transport; restore first reads the trashed entry and submits its deleteOperationId as the Host CAS input"
  ],
  "subdirectories": {
    "nodes": "Custom React Flow node components (e.g., VideoNode, TextNode). The visual representation of data.",
    "copilot": "Components for the Chatbot/AI Assistant sidebar interface."
  },
  "key_files": {
    "SyncRecoveryDialog.tsx": "Shared Radix recovery dialog mounted by ProjectEditor. Rejected edits use the local archive/reload workflow; a Host project-load failure shows the supplied cause and retries the existing sync connection without clearing local edits or changing plugin versions.",
    "CanvasCreateMenuItems.tsx": "Shared creation actions for the canvas plus menu and empty-pane double-click menu; uses shared Radix menu items and action submenu.",
    "AppPage.tsx": "Shared authenticated content-page inset, semantic narrow/standard/wide width contract, AppPageHeader title/description/action rhythm, and AppBreadcrumb hierarchy navigation; full-bleed editors remain outside it.",
    "BrandAsset.tsx": "Registry-backed renderer for Clash-owned identity, feature, and state artwork. Consumers select a semantic asset name instead of repeating public paths.",
    "AppFeedback.tsx": "The single transient feedback queue and toast viewport. Errors persist until dismissed; warning/info/success timing and reduced-motion behavior are centralized here, and activity events adapt into this provider instead of rendering another viewport.",
    "ProjectEditor.tsx": "The primary workspace component integrating React Flow and the Toolbar. Project Asset membership comes from Project Loro; resolved Host projections refresh on each membership revision and retry the same revision after transient Host disconnects.",
    "BrowserSurface.tsx": "Backchat-derived Electron webview surface with navigation, persistent page state, and element/region annotations routed into the shared Agent annotation contract.",
    "ProjectBrowserSurfaces.tsx": "Keeps every Project sidebar browser tab mounted while exposing only the selected webview surface; memoized per-tab children and stable event callbacks preserve page state without rebinding on unrelated workspace updates.",
    "DashboardComposerContext.tsx": "Route-stable Dashboard draft and Project/Skill reference state, including the shared drag-and-drop boundary and focus handoff.",
    "DashboardComposerDock.tsx": "Persistent Home/Marketplace bottom Composer dock with an external compact searchable Project scope tag, in-surface Skill references, shared Backchat runtime controls, and one same-document transition into Project.",
    "ProjectCard.tsx": "Project-browser card that loads one Host-derived Main canvas WebP per revision and keeps the lightweight geometry SVG as a no-media loading/error fallback.",
    "MarketplacePluginDetail.tsx": "Canonical Marketplace plugin detail surface with shared breadcrumb navigation, desktop trackpad back gesture, and registry-declared capabilities.",
    "usePageHistorySwipe.ts": "Detail-page back gesture using the shared wheel primitive; preserves browser-native navigation, scrolling, editable regions, and pinch zoom.",
    "marketplaceRouting.ts": "Single canonical path builder for action and skill detail links used by Marketplace cards and the Home featured feed.",
    "desktopShellLayers.ts": "Semantic desktop shell elevation order shared by Dashboard task surfaces and transient sidebar preview layers.",
    "ChatbotCopilot.tsx": "The AI chat interface component.",
    "copilot/AgentAnnotationBlock.tsx": "Special composer block for editable agent-addressable review notes from Canvas, Timeline, and Director Stage.",
    "copilot/AgentAnnotationContextMenu.tsx": "Shared context-menu action that sends an addressable workspace object into the composer.",
    "copilot/AgentSelectionAnnotationOverlay.tsx": "Captures native text selections inside creative surfaces, renders numbered anchored highlights, and queues an optional comment through the shared agent annotation contract.",
    "PresenceAwarenessContext.tsx": "Stable external presence store; canvas cards subscribe to per-node peer arrays and attribution subscribes to a user name, so unrelated cursor broadcasts do not rerender the card tree.",
    "LoroSyncContext.tsx": "React Context provider for the Loro CRDT sync engine.",
    "CanvasViewContext.tsx": "Navigation context for generator-free Canvas View nodes that open their referenced draft Canvas.",
    "nodes/CanvasViewNode.tsx": "View-only Canvas plugin card; it opens a referenced Canvas and intentionally exposes no Generator action or output handle.",
    "VideoEditorContext.tsx": "State management for the video editing timeline.",
    "GlobalAssetsClient.tsx": "Personal Global library UI over the shared import, trash, and observed-operation restore lifecycle.",
    "ProjectWorkspaceSurfaces.tsx": "Project Asset preview and native Host Timeline editor. Saves await accepted revisions; local edit detection excludes media discovery, and export waits for pending edits.",
    "timelineMediaInputs.ts": "Resolve native Timeline Project Asset refs and available Canvas media for preview; Canvas nodes are navigation hints, not Asset storage authority.",
    "nodes/NativeMediaReference.tsx": "Displays native Generator media input facts directly from Project Asset projections without requiring Canvas nodes. Removal identifies a specific slot/itemKey, preserving repeated references to the same Asset.",
    "nodes/NativeOrderedPrompt.tsx": "Edits existing ordered Model prompt text sections through the native draft queue. Input positions remain explicit; text changes reconstruct the flat prompt from the same contentParts.",
    "nodes/TextDocumentNode.tsx": "Read-only Canvas placement and shared Dialog for the exact text Document result. Uses the same revision reader as the sidebar rather than editable legacy text content.",
    "TextDocumentReadSurface.tsx": "Sidebar text-result reader over an exact Document revision. ProjectEditor selects this surface for Document-backed nodes and retains the existing editor for ordinary authored text."
  },
  "architecture_notes": "Keep ProjectEditor focused on workspace composition. Asset Preview/Edit, thumbnails, media resolution, and action clients belong in src/features/assets; shared action semantics belong in @clash/shared-types/actions. Home and in-session Composer surfaces reuse the same Backchat-derived editor, model/permission controls, send/voice state, and content-driven resize logic; Clash extends that core only through capability-scoped Project/Canvas/Timeline context and density tokens. Authenticated content pages must use AppPage/AppPageInset instead of page-local outer padding; Settings applies it inside its scroll pane, while Project/editor workspaces remain full bleed."
}
