{
  "description": "Runtime clients and execution helpers shared by Clash product surfaces.",
  "modules": {
    "src/browser.ts": "Browser/Worker exports; never import filesystem, daemon discovery, or process helpers here.",
    "src/project-host-http.ts": "Single HTTP transport for existing project Host commands. Browser callers inject their runtime request; Node callers supply the discovered endpoint.",
    "src/project-host-client.ts": "Node workspace-marker and Host endpoint resolution, reusing project-host-http transport.",
    "src/timeline-client.ts": "Native Timeline Host projection client. Keep the returned observation with each draft; never rotate an old draft's CAS evidence after a background refresh.",
    "src/generator-client.ts": "Generic Generator/Revision/Action Run/Output Commit HTTP client.",
    "src/document-client.ts": "Exact Project Document revision body reads through the existing Host API, shared by MCP Assets and CLI assets documents. Requires a revision identity and preserves structured HTTP errors.",
    "src/logging.ts": "Browser-safe versioned diagnostics, structured Error context, bounded sanitization and duplicate summaries. No filesystem or Node imports.",
    "src/observability.ts": "Node JSONL sinks and stdio capture; bounded rotation, process run identity, console severity, line framing and startup-only forwarding.",
    "src/log-reader.ts": "Read-only local diagnostic query across desktop and Host rotations; legacy compatibility, event summaries and original file/line locations."
  }
}
